window.NOC=window.NOC||{};
NOC.Configuracion=(()=>{async function render(){const env=await NOC.API.list("tipos_envio",{order:"orden",ascending:true});document.getElementById("viewContainer").innerHTML=`<div class="grid"><div class="card col-12"><div class="toolbar"><div><h2>Tipos de envío</h2><p class="muted">Tarifas para proformas y facturas.</p></div><button class="btn btn-primary" onclick="NOC.Configuracion.openEnvio()">+ Nuevo tipo</button></div><div class="table-wrap"><table><thead><tr><th>Nombre</th><th>Empresa</th><th>Precio</th><th>IVA</th><th>Orden</th><th>Predeterminado</th><th>Activo</th><th>Acciones</th></tr></thead><tbody>${env.map(e=>`<tr><td>${NOC.App.esc(e.nombre)}</td><td>${NOC.App.esc(e.empresa||"")}</td><td>${NOC.App.money(e.precio)}</td><td>${e.iva_porcentaje}%</td><td>${e.orden}</td><td>${e.predeterminado?"Sí":""}</td><td>${e.activo?"Sí":"No"}</td><td class="actions"><button class="btn btn-small" onclick="NOC.Configuracion.openEnvio('${e.id}')">Editar</button><button class="btn btn-small btn-danger" onclick="NOC.Configuracion.delEnvio('${e.id}')">Borrar</button></td></tr>`).join("")||`<tr><td colspan="8" class="empty">Sin tipos de envío.</td></tr>`}</tbody></table></div></div></div>`}async function openEnvio(id){const e=id?await NOC.API.one("tipos_envio",id):{activo:true,iva_porcentaje:21,orden:10};NOC.App.modal(`<div class="modal-head"><strong>${id?"Editar":"Nuevo"} tipo de envío</strong><button class="icon-btn" onclick="NOC.App.closeModal()">×</button></div><div class="modal-body"><form id="envForm" class="form-grid"><div class="field f6"><label>Nombre</label><input name="nombre" value="${NOC.App.esc(e.nombre||"")}"></div><div class="field f6"><label>Empresa</label><input name="empresa" value="${NOC.App.esc(e.empresa||"")}"></div><div class="field f3"><label>Precio</label><input name="precio" type="number" step="0.01" value="${Number(e.precio||0)}"></div><div class="field f3"><label>IVA %</label><input name="iva_porcentaje" type="number" step="0.01" value="${Number(e.iva_porcentaje||21)}"></div><div class="field f3"><label>Orden</label><input name="orden" type="number" value="${Number(e.orden||10)}"></div><div class="field f3"><label>Activo</label><select name="activo"><option value="true" ${e.activo?"selected":""}>Sí</option><option value="false" ${!e.activo?"selected":""}>No</option></select></div><div class="field f4"><label>Predeterminado</label><select name="predeterminado"><option value="false" ${!e.predeterminado?"selected":""}>No</option><option value="true" ${e.predeterminado?"selected":""}>Sí</option></select></div></form></div><div class="modal-foot"><button class="btn btn-primary" onclick="NOC.Configuracion.saveEnvio('${id||""}')">Guardar</button></div>`)}async function saveEnvio(id){const f=Object.fromEntries(new FormData(document.getElementById("envForm")));f.precio=Number(f.precio||0);f.iva_porcentaje=Number(f.iva_porcentaje||21);f.orden=Number(f.orden||0);f.activo=f.activo==="true";f.predeterminado=f.predeterminado==="true";if(f.predeterminado){const {error}=await NOC.API.db().from("tipos_envio").update({predeterminado:false}).neq("id","00000000-0000-0000-0000-000000000000");if(error)throw error}if(id)await NOC.API.update("tipos_envio",id,f);else await NOC.API.insert("tipos_envio",f);NOC.App.closeModal();render()}async function delEnvio(id){if(confirm("¿Borrar tipo de envío?")){await NOC.API.remove("tipos_envio",id);render()}}return{render,openEnvio,saveEnvio,delEnvio}})();