window.NOC=window.NOC||{};
NOC.Gastos=(()=>{async function render(){const rows=await NOC.API.list("gastos",{order:"fecha",ascending:false});document.getElementById("viewContainer").innerHTML=`<div class="card"><div class="toolbar"><div></div><button class="btn btn-primary" onclick="NOC.Gastos.openEditor()">+ Nuevo gasto</button></div><div class="table-wrap"><table><thead><tr><th>Fecha</th><th>Empresa</th><th>Tipo</th><th>Factura</th><th>Cantidad</th><th>Acciones</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${r.fecha}</td><td>${NOC.App.esc(r.empresa)}</td><td>${NOC.App.esc(r.tipo_gasto||"")}</td><td>${NOC.App.esc(r.numero_factura||"")}</td><td>${NOC.App.money(r.cantidad)}</td><td class="actions"><button class="btn btn-small" onclick="NOC.Gastos.openEditor('${r.id}')">Editar</button><button class="btn btn-small btn-danger" onclick="NOC.Gastos.del('${r.id}')">Borrar</button></td></tr>`).join("")||`<tr><td colspan="6" class="empty">No hay gastos.</td></tr>`}</tbody></table></div></div>`}async function openEditor(id){const r=id?await NOC.API.one("gastos",id):{fecha:NOC.App.today()};NOC.App.modal(`<div class="modal-head"><strong>${id?"Editar gasto":"Nuevo gasto"}</strong><button class="icon-btn" onclick="NOC.App.closeModal()">×</button></div><div class="modal-body"><form id="gastoForm" class="form-grid"><div class="field f3"><label>Fecha</label><input name="fecha" type="date" value="${r.fecha||NOC.App.today()}"></div><div class="field f5"><label>Empresa</label><input name="empresa" value="${NOC.App.esc(r.empresa||"")}"></div><div class="field f4"><label>Tipo de gasto</label><input name="tipo_gasto" value="${NOC.App.esc(r.tipo_gasto||"")}"></div><div class="field f4"><label>Cantidad</label><input name="cantidad" type="number" step="0.01" value="${Number(r.cantidad||0)}"></div><div class="field f4"><label>Nº factura</label><input name="numero_factura" value="${NOC.App.esc(r.numero_factura||"")}"></div><div class="field f12"><label>Observaciones</label><textarea name="observaciones">${NOC.App.esc(r.observaciones||"")}</textarea></div></form></div><div class="modal-foot"><button class="btn btn-primary" onclick="NOC.Gastos.save('${id||""}')">Guardar</button></div>`)}async function save(id){const f=Object.fromEntries(new FormData(document.getElementById("gastoForm")));f.cantidad=Number(f.cantidad||0);if(id)await NOC.API.update("gastos",id,f);else await NOC.API.insert("gastos",f);NOC.App.closeModal();render()}async function del(id){if(confirm("¿Borrar gasto?")){await NOC.API.remove("gastos",id);render()}}return{render,openEditor,save,del}})();